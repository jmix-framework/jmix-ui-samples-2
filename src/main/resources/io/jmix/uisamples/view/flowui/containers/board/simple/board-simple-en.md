The `board` component is most often used for building dashboards and responsive views.\
The component can contain `boardRow` rows, which contain up to four other UI components (including other `boardRow`).
