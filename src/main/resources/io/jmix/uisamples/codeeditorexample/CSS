.text-layer {
    font: 12px Monaco, "Courier New", monospace;
    font-size: 3vmin;
    cursor: text;
}

.blinker {
    animation: blink 1s linear infinite alternate;
}

@keyframes blink {
    0%, 40% {
        opacity: 0; /*
        */
        opacity: 1
    }

    40.5%, 100% {
        opacity: 1
    }
}

@document url(http://jmix.io/), url-prefix(http://jmix.io/uisamples),
domain(jmix.io), regexp("https:.*") /**/
{
    /**/
    img[title]:before
    {
        content: attr(title) "\AImage \
            retrieved from"
        attr(src); /*
            */
        white-space: pre;
        display: block;
        background: url(ASDASD); "err
    }
}

@viewport {
    min-zoom: 1;
    max-zoom: 200%;
    user-zoom: fixed;
}
